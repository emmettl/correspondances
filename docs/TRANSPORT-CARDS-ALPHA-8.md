# Shared transport cards · alpha.8

All consumed `@motionstudies/*` packages are pinned to `0.1.0-alpha.8` with npm registry integrity hashes.

Selected stations show French-labelled matrix rail cards. The station movement counter remains visible, and selecting a departure highlights its train. Loaded schedules are explicitly described as non-live.
